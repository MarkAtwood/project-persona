{
  "id": "persona-qbnm.6",
  "title": "tailscale: assert trust and ship. oidc: verify the JWKS.",
  "description": "DECIDED 2026-09-15, Mark: WE ASSERT TRUST IN TAILSCALE. No calibration\nframework, no negotiation, no waiting on persona-ux32. tailscaled is a trusted\nlocal authority and hired believes it.\n\nThat is not a concession, it is the rule this codebase already follows.\nPlatformIdentity's doc comment says it outright for the kernel -- \"there is no\nkey material and no signature here because there are none to have: the operating\nsystem is the entire authority\" -- and unix.rs ships on exactly that basis.\ntailscaled is the same shape: a daemon the user installed and runs, on a Unix\nsocket under /var/run that only their account can reach. Refusing to believe it\ndoes not make the identity local, it makes Tailscale unsupported.\n\nSo this bead is no longer a decision. It is two independent pieces of ordinary\nwork, and they should be done separately because they share nothing.\n\nTAILSCALE -- assert and ship. Needs no new dependency and no enrolment.\n  Evidence in the PlatformIdentity family: no secret, no challenge, bound by\n  CONTENT MATCH (the login name tailscaled reported must match the candidate),\n  dated by an instant the daemon stamps when it asks. Never let the answer date\n  itself. Tier is Iaa2 per SPEC-HIRE, and that is honest rather than generous:\n  Tailscale's IdP really did verify the user against Google or GitHub or an OIDC\n  provider, and tailscaled is relaying a verification that actually happened.\n  Presence stays NONE -- a network identity is not a human at a keyboard.\n\nOIDC -- verify the signature we are already declining to verify. This one is\n  not a trust-assertion question at all. A cached ID token carries iss and a\n  real signature, and the issuer's JWKS is fetchable (the network is settled --\n  see the personad-may-use-the-network memory, where OIDC JWKS was one of the\n  three reasons). Today oidc.rs checks none of it. So the work is a JWKS fetch\n  plus a cache plus a verification, which is what VerifiedToken's ponytail\n  comment in claim.rs already names as its upgrade path: add a verifying\n  constructor \"next to the JWKS cache that makes it possible -- and nowhere\n  else\". Freshness still does not come from the artifact: bound it with\n  hire_max_age and the token's own auth_time, per VerifiedToken's warning\n  against stamping SystemTime::now().\n\nWHAT THIS LEAVES persona-ux32: the case of a NEW external attestor nobody has\ndecided about yet. That is where calibration belongs and it is real -- but it\ngates none of the sources shipping today, which is why ux32 drops to P2.",
  "design": "Note this interacts with persona-3tly.1 (presence is hearsay): the general problem of an assertion that cannot be carried to the consumer. A source we cannot challenge is the same defect one layer earlier -- an assertion we cannot verify on arrival.",
  "acceptance_criteria": "tailscale proves, bound by content match against the candidate and dated by the daemon, at Iaa2 with no presence. oidc verifies the ID token against the issuer's JWKS before producing evidence. The two land separately.",
  "notes": "A THIRD MEMBER OF THIS CLASS, AND IT SPLITS THE CLASS USEFULLY: persona-lbdx, a signed identity attestation on removable media. Like tailscale and oidc it cannot answer a challenge -- a static signed file has nothing to bind to a nonce generated this second.\n\nBut it separates two things this bead currently treats as one. tailscale's trust basis is UNVERIFIABLE (tailscaled's word, which nothing here can check) and oidc's is UNVERIFIED (a signature we could check against the issuer's JWKS and do not). lbdx's is VERIFIABLE AND VERIFIED, against a key the user deliberately enrolled per persona-08sz.\n\nSo 'cannot answer a challenge' is not one problem. It is two:\n  WHOSE WORD IS IT       -- answerable by enrolment and a signature check\n  WHEN WAS IT TRUE       -- not answerable by the artifact at all\n\nlbdx answers the first and takes the second from an event the DAEMON observed\n(the mount), which is exactly how PlatformIdentity already works: bind by\ncontent match, stamp the instant locally, never let the artifact date itself.\n\nThat decomposition is probably the answer for oidc too -- verify the JWKS\nsignature for the first question, and stop pretending the second is answered by\nthe token's presence on disk. It is not the answer for tailscale, which fails\nthe first question and so needs persona-ux32's calibration rather than a\ncryptographic check.",
  "status": "closed",
  "priority": 1,
  "issue_type": "task",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-15T20:50:30.981916543Z",
  "created_by": "agent",
  "updated_at": "2026-09-16T01:37:15.964797529Z",
  "closed_at": "2026-09-16T01:37:15.964797529Z",
  "close_reason": "Tailscale half landed in 510c875: prove() re-reads tailscaled's status and requires the same identity, yielding the new Evidence::DaemonAssertion (Iaa2, no presence). Trust in the local daemon is asserted, per the assert-trust-in-local-daemons decision, and calibration was not built. The oidc half is a different question -- a verifier to write, not a trust to assert -- and is re-filed with the measurements behind it, including the finding that auth_time is usually absent from cached gcloud and Azure tokens, which is the real blocker rather than the JWKS fetch.",
  "labels": [
    "attestor",
    "driver",
    "security"
  ],
  "dependencies": [
    {
      "issue_id": "persona-qbnm.6",
      "depends_on_id": "persona-qbnm",
      "type": "parent-child",
      "created_at": "2026-09-15T20:50:31.013747581Z",
      "created_by": "agent",
      "metadata": "{}"
    }
  ]
}
