{
  "id": "persona-5s4b.126",
  "title": "persona log, install-service and watch give platform-wrong advice",
  "description": "Three low-blast-radius CLI issues found by the discovery lens during the .102/.103 pass. Grouped because each is wrong advice rather than fabricated work, and the fixes are independent one-liners.\n\n1. main.rs:499-504 -- 'persona log' gives only the journalctl recipe, but install_service on macOS routes the daemon's output to ~/Library/Logs/personad.log and the plist comment records that launchd sends it nowhere else. A macOS operator runs a command that does not exist on the platform, gets nothing, and can reasonably conclude personad keeps no audit trail, while the file holding SPIFFE IDs, attestor sources and assurance levels sits unmentioned. The help line is honest that this is advice ('show how to view'), so it is wrong advice, not a fake action.\n\n2. main.rs:506-533 -- install_service guards only the macOS branch with cfg; the systemd branch is the fallthrough for EVERY other target, not for Linux specifically. On FreeBSD or illumos it writes ~/.config/systemd/user/personad.service, prints 'installed personad.service, run: systemctl --user enable --now personad', and exits 0 on a host with no systemd. The operator's next step fails loudly, unlike the stub beads, but a success noun is still printed over an install that cannot work.\n\n3. main.rs:486-497 -- chrono_timestamp derives hh:mm:ss from Unix epoch seconds mod 86400, so persona watch labels each line with a UTC time-of-day carrying no zone marker and no date. An operator correlating watch output against journald, which renders local time by default, mis-aligns the two by the UTC offset during an incident, with no date to disambiguate across midnight.",
  "acceptance_criteria": "log names the macOS path on macOS; install-service refuses unknown init systems instead of assuming systemd; watch timestamps carry a zone marker.",
  "status": "open",
  "priority": 3,
  "issue_type": "bug",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-15T03:38:01.710365506Z",
  "created_by": "agent",
  "updated_at": "2026-09-15T03:38:01.710365506Z",
  "labels": [
    "review"
  ],
  "dependencies": [
    {
      "issue_id": "persona-5s4b.126",
      "depends_on_id": "persona-5s4b",
      "type": "parent-child",
      "created_at": "2026-09-15T03:38:01.725324331Z",
      "created_by": "agent",
      "metadata": "{}"
    }
  ]
}
