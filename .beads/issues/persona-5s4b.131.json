{
  "id": "persona-5s4b.131",
  "title": "A second personad silently steals the socket from a running one",
  "description": "Found while fixing persona-5s4b.35, which assumed the opposite. That bead says a caller should be able to see io::ErrorKind::AddrInUse and conclude 'another personad is running -- exit quietly'. It cannot, because that case never arises.\n\npersona-grpc/src/server.rs bind_listener unlinks any existing socket before binding. So when a second personad starts while a first is running:\n- the second unlinks the first's socket and binds its own at the same path\n- bind(2) succeeds; there is no AddrInUse\n- the first daemon keeps its listener open on an unlinked inode, serving only connections already accepted, and never learns it was displaced\n- every new client reaches the second daemon\n\nThe two daemons hold DIFFERENT ephemeral signing keys and DIFFERENT pseudonym_ikm (personad/src/main.rs:32 and persona-core/src/signer.rs:108), so consumers silently re-home to a new set of pseudonyms and a new JWT authority mid-session, with nothing logged beyond the existing 'removed stale socket' warning -- which reads as routine cleanup.\n\nThe unlink is deliberate and correct for the case it was written for: a socket left behind by a daemon that died. The gap is that an unlinked socket and a live one are indistinguishable by path alone.\n\nFix direction (fork, not decided here): the usual answer is an advisory lock on a separate file (flock on $XDG_RUNTIME_DIR/persona/personad.lock) taken before the unlink -- a held lock means a live daemon, a stale lock is released by the kernel when its holder dies, which is exactly the distinction the socket file cannot make. Alternative: connect(2) to the existing socket first and refuse if something answers, which needs no new file but races and treats a hung daemon as live. Either way the decision 'second instance refuses' vs 'second instance takes over' is a behaviour choice for Mark, and interacts with the settled rule that a command which did no work exits non-zero.\n\nNot a regression: predates persona-5s4b.35 and 56efa94.",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "owner": "mark@reviewcommit.com",
  "created_at": "2026-09-15T05:24:08.931094699Z",
  "created_by": "agent",
  "updated_at": "2026-09-15T16:00:09.89962874Z",
  "closed_at": "2026-09-15T16:00:09.89962874Z",
  "close_reason": "Decided by Mark 2026-09-15 and implemented in 4632471: the SECOND instance refuses; the running daemon is never touched. Detection is an exclusive non-blocking flock, chosen over a connect() probe (which races -- two daemons starting together both probe, both find nothing, both bind) and over documenting the takeover (which cannot even warn accurately, since without a liveness check you never learn you displaced anyone).\n\nWhy flock is the right primitive and not just a mechanism: the kernel releases it when its holder dies, so it answers exactly the question the socket path cannot -- is the daemon that made this still alive? A crashed daemon's lock is already free, so stale-socket cleanup keeps working unchanged.\n\nDetails worth keeping:\n- The lock is taken BEFORE the unlink. After it would be pointless.\n- The lock is keyed to the socket path (\u003csocket\u003e.lock), NOT its directory. A directory-wide lock breaks the integration tests, which bind a dozen sockets under /tmp, and the real invariant is one daemon per socket anyway.\n- The lock file is never unlinked. Removing it races a process that has already opened the same path and would leave it locking an unlinked inode.\n- The refused instance exits non-zero with the message on stderr, matching the settled rule for a command that did no work.\n- No new dependency: libc was already a persona-grpc dependency. flock(2) exists on Darwin too, so the shipped LaunchAgent is covered.\n\nVerified with real processes, not only unit tests: a second personad exits 1 with 'another personad is already serving \u003cpath\u003e' while the first keeps serving with its socket intact; a clean exit is reclaimed; and a SIGKILLed daemon that leaves BOTH socket and lock behind is also reclaimed, because the kernel released the flock. Five mutations verified, including the naive wrong fix (refuse whenever the socket file exists), which the reclaim-after-crash test catches.\n\nFollow-on note, not acted on: systemd Restart=on-failure would restart a refused instance every 5s. A user unit is a singleton so systemd will not double-start it; the realistic collision is a hand-run daemon, which simply prints and exits. If a hand-run daemon ever holds the socket while the unit starts, the unit restart-loops -- arguably the correct alarm, but RestartPreventExitStatus= is there if that proves annoying.",
  "labels": [
    "review"
  ],
  "dependencies": [
    {
      "issue_id": "persona-5s4b.131",
      "depends_on_id": "persona-5s4b",
      "type": "parent-child",
      "created_at": "2026-09-15T05:24:08.953824225Z",
      "created_by": "agent",
      "metadata": "{}"
    }
  ]
}
